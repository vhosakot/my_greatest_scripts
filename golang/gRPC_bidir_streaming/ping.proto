syntax = "proto3";
package ping;

option go_package = "./pb_files";

service PingService {
  // Bidirectional stream: client and server can send multiple messages
  rpc Chat(stream PingRequest) returns (stream PingResponse);
}

message PingRequest {
  string payload = 1;
}

message PingResponse {
  string payload = 1;
}
